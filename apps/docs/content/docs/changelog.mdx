---
title: Changelog
description: Documentation updates synced from the ValidusBot Lua spec and Scripts/core
---

User-facing record of documentation syncs. Newest entries first. Each run of the docs update workflow prepends a dated section covering **Added**, **Changed**, and **Removed**.

## 2026-09-22

Synced local `D:\Pobrane\lua-llm-scripting-spec.md` (not GitHub) and installed `Scripts/core` (31 Lua files). Regenerated the API reference (~1611 documented functions).

### Added

- Spec section **Extended native Game API** (catalogue helpers, tactics/patterns, filtered pathfinding, diagnostics) — summarized in Communication guide
- Extended Walker Lua API: Auto Explore mask paint/replace/clear, supplies checker, combat egress masks, waypoint reorder/selected helpers, recorder mode/direction, leave-lure name lists
- Walker movement observers: `WalkerEvent.MOVEMENT_CONFIRMED` / `MOVEMENT_REJECTED`, `Cavebot.OnMovementConfirmed` / `OnMovementRejected`
- HUD pointer callbacks: `ScreenText` / `ScreenImage` `SetPointerCallbacks` (+ mouse down/up/leave) and `Engine.HUD.SetPointerCallbacks`
- Optional `allowSpeculativeUnknown` on `Map.FindPath` / `Minimap.FindPath` (and matching native `Game.FindPath*` variants)

### Changed

- `os.exit()` hard-rule wording: audit-logs and immediately terminates without logout prompt or normal cleanup
- PathFindFlags behavior notes (diagonal cost, creature/walkable interactions) and corrected `Map.FindPath` example to use `MapPathResult`
- Auto Explore docs: painted areas are editable from Lua while Walker is stopped (no longer World Map only)
- Regenerated API pages (notably cavebot, engine, map, minimap, hud, constants, event-proxies)
- Guides updated: Safety, Map & Navigation, Cavebot, Constants, Events & HUD, Table Contracts, Communication

### Removed

- Nothing removed from the public core module set this sync

## 2026-09-18

Synced upstream `lua-llm-scripting-spec.md` (unchanged vs prior sync) and local `Scripts/core` (31 Lua files). Regenerated the API reference with core-ahead merge for modules/functions not yet in the upstream appendix.

### Added

- Opcode packet proxies: `PacketEventProxy`, `IncomingPacketProxy`, `OutgoingPacketProxy`, and client/server convenience subclasses
- Expanded `Self` character-flag helpers (`HasCharacterFlag`, `IsPzBlocked`, hex / Goshnar / agony / powerless / mentor checks) and matching `CharacterFlag` values
- `CurrentClientOpcodes` / `CurrentGameServerOpcodes` for modern protocol packet IDs
- Shared profile helpers on `Engine.AmmoRefill`, `MagicShooter`, `Targeting`, and `EquipmentManager` (`GetProfiles`, `GetActiveProfile`, `NextProfile`, …)

### Changed

- API generator merges installed core functions/constants when they are ahead of the upstream appendix
- Regenerated API pages (notably self, event-proxies, constants, engine)
- Guides updated: Self, Events & HUD, Storage, Engine Features, Table Contracts, Constants

### Removed

- Nothing removed from the public core module set this sync

## 2026-08-25

Synced upstream `lua-llm-scripting-spec.md` and local `Scripts/core` (31 Lua files, unchanged set). Regenerated the API reference with richer typed appendix signatures.

### Added

- Event/hook **critical-path** rule: constant-time, non-yielding callbacks that only feed a stable `Module.Every` worker (coalesced slot or hard-bounded queue)
- Safe packet-to-worker example (`message_contains` filter + `Http.PostJson` in the worker)
- [Table Contracts](/docs/guides/table-contracts) guide for exact, case-sensitive field names (`itemId`, `slotIndex`, `Directions`, …)

### Changed

- Hard rule 11 and safety checklist / LLM generation rules now require critical-path callbacks and exact table contracts
- Runtime, Events & HUD, Cavebot interceptors, Examples, and template updated for enqueue-and-return + `Cavebot.Defer` worker pattern
- Regenerated API pages (notably cavebot, engine, hud, spells, item, features) from typed appendix records

### Removed

- Nothing removed from the public core module set (no `.lua` files added or deleted this sync)

## 2026-08-21

Synced upstream `lua-llm-scripting-spec.md` and local `Scripts/core` (31 Lua files). Regenerated the API reference and updated narrative guides.

### Added

- Profile file APIs: `Engine.Settings.Save` / `Load` (`.json`) and `Cavebot.Save` / `Load` (`.validuswpt`), including feature/bundle selection and path search rules
- Walker Special Areas (`AddSpecialArea`, feature masks via `SpecialAreaFeature`) and Auto Explore navigation mode, settings, status, and connectors
- Lure kiting distance/count getters and setters (`SetKitingPreferredFarthestDistance`, …)
- `SpecialAreaFeature` constants on the constants guide and regenerated API pages

### Changed

- Runtime circuit-break rule: three consecutive unpreempted resumes of at least 5 ms, or one unpreempted 50 ms resume (deadline misses remain telemetry)
- Map pathfinding notes for teleport/floor-change tiles; `Self.Step` documented as dispatch-only with position confirmation
- Sound name resolution: Alarms folder then script folder; absolute `file_path`; `PlayBotSound` accepts name or full path
- Regenerated API pages for cavebot, engine, constants, map, minimap, self, and sound

### Removed

- Nothing removed from the public core module set (no `.lua` files added or deleted this sync)

## 2026-08-07

Synced upstream `lua-llm-scripting-spec.md` (`58d29b1`) and local `Scripts/core` (31 Lua files). Regenerated the API reference and updated narrative guides.

### Added

- Runtime docs for failure isolation, ownership/budgets, sandbox file/`require` limits, and `Engine.Scripter` lifecycle
- `Time.MonotonicMs()` guidance and LLM API-selection workflow on the safety page
- Cross-script `Storage.Shared` / `Storage.SharedForCharacter` (`Update`, `OnChanged`)
- Cavebot Observe*/Intercept*/Defer APIs, Action lifecycle (`OnActionStarted` / `OnActionCompleted`), and leave-lure player modes
- Native `Events` scheduling/registration notes; Sound Smart/`PlayAndWait` helpers; HUD GIF/async image limits
- Examples for shared storage counters and Cavebot observe + Action lifecycle
- This changelog page

### Changed

- Hard rules / checklist: narrow `pcall`, monotonic time, PASS/FAIL diagnostics, `init`/`terminate`
- Engine / Magic Shooter guide: strict vector order, Momentum, stance/x64 capability checks
- Module scheduling: `Every`/`After` registry vs raw `Module.New`
- Minimap guide: `GetTilePixelColor` / `IsWalkableByColor` documented as nil stubs
- Lure `SetOption` values documented (`0` Start/End, `1` Dynamic, `2` Kiting)
- Regenerated API pages for cavebot, engine, features, hotkeys, hud, module, self, sound, storage, minimap, constants

### Removed

- Nothing removed from the public core module set (no `.lua` files added or deleted this sync)
